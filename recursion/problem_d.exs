defmodule ProblemD do
  @moduledoc """
  Recusion functions
  """

  @doc """
  tail recursive fibonacci
  """

  def  fib(n), do: fib(n, 1, 0) # base case
  defp fib(0, _, acc) do # end case
  end
  defp fib # recursive step
end
